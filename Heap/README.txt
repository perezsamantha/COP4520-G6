README

This just doesn't work so don't even try. To compile it you would just have to compile it like any other java file, with javac *.java in this directory.

To instantiate it you just have to use Heap()

To popmin you just heap.popMin()

To insert you just heap.insert(int key, int value)